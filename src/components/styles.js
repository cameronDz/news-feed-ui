const appFooterStyles = {
  appFooterWrapper: {
    bottom: "0",
    margin: "-36px auto 0 auto",
    maxHeight: "24px",
    maxWidth: "1440px",
    textAlign: "center",
  },
};

const rssStyles = {
  alignItems: "center",
  display: "flex",
  flexDirection: "column",
  textAlign: "center",
};

export { appFooterStyles, rssStyles };
